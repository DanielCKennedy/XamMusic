<?xml version="1.0" encoding="utf-8" ?>
<pages:PopupPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:pages="clr-namespace:Rg.Plugins.Popup.Pages;assembly=Rg.Plugins.Popup"
             xmlns:animations="clr-namespace:Rg.Plugins.Popup.Animations;assembly=Rg.Plugins.Popup"
             x:Class="XamMusic.Controls.QueuePopup"
             BackgroundColor="#F0000000"
             CloseWhenBackgroundIsClicked="False"
             HasSystemPadding="True">
    <StackLayout Orientation="Vertical" Padding="10, 5" Spacing="3">
        <StackLayout Orientation="Horizontal" Spacing="5" VerticalOptions="Start" HorizontalOptions="Center">
            <Button Text="Shuffle" Command="{Binding ShuffleCommand}" />
            <Button Text="Clear" Command="{Binding ClearQueueCommand}"/>
        </StackLayout>
        <ListView x:Name="QueueListView" ItemsSource="{Binding Queue}"
                  SelectedItem="{Binding SelectedSong, Mode=TwoWay}" VerticalOptions="FillAndExpand">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextCell Text="{Binding Title}" Detail="{Binding Artist}"/>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <Button BackgroundColor="Red" TextColor="Black" Text="X" VerticalOptions="End" Clicked="ClosePopup" HorizontalOptions="Center"/>
    </StackLayout>
</pages:PopupPage>